# Module 2: Удосконалення ПЗ та повторне тестування (Task 2) (+fix)

## Що змінено у версії v1.1:
1. **Новий модуль експорту:** Реалізовано функціонал збереження персоналізованих звітів у форматі `JSON` (`report_user_[id].json`).
2. **Оновлення інтерфейсу:** Додано нову інтерактивну кнопку "Експортувати звіт у JSON" з відповідною помаранчевою кольоровою гамою.
3. **Регресійне тестування:** Проведено повторне тестування, яке підтвердило, що додавання модуля експорту не порушило роботу валідації та алгоритму KNN.
4. **Актуалізація версії:** Уся документація, конфіги та заголовки додатку переведені на версію **v1.1**.

## Структура вихідного файлу JSON-звіту:
```json
{
    "app": "SmartRec",
    "version": "1.1",
    "target_user_id": 1,
    "similar_user_id": 4,
    "recommended_products": [102, 105]
}